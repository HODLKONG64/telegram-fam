def main() -> None:
    print(
        "[ingest-brain] disabled. Telegram now reads shared SAM memory directly from R2 "
        "(sam-memory/sam-memory.json)."
    )


if __name__ == "__main__":
    main()
